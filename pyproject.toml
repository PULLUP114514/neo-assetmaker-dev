[project]
name = "neo-assetmaker"
version = "2.2.0"
description = "明日方舟通行证素材工具箱"
readme = "README.md"
# VapourSynth 进程内渲染要求 3.12+：捆绑的 vapoursynth wheel 是 cp312-abi3
# (稳定 ABI 下限 3.12)，其 .pyd 引用的 PyObject_Vectorcall / PyType_FromMetaclass
# 等符号在 3.11 的 python3.dll 中不存在，import 会以 ERROR_PROC_NOT_FOUND 失败。
requires-python = ">=3.12"
dependencies = [
    "PyQt6>=6.5.0",
    "PyQt6-Fluent-Widgets>=1.5.0",
    "PyOpenGL>=3.1.0",
    "opencv-python>=4.8.0",
    "Pillow>=10.0.0",
    "numpy>=1.24.0",
    "jsonschema>=4.17.0",
    "thefuzz>=0.22.0",
    "python-Levenshtein>=0.25.0",
    "requests>=2.32.0",
    # 扩展模块依赖
    "httpx>=0.27",
    "keyring>=25",
    "platformdirs>=4",
    "fido2>=2.1",
    "pyusb>=1.3",
]

[dependency-groups]
dev = [
    "cx-Freeze>=7.0.0,<8.0.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["gui", "core", "config", "utils", "_mext"]

[tool.uv]
required-version = ">=0.10.10"
constraint-dependencies = ["pyqt5-qt5<=5.15.2"]
